
<tr>
	<td><%= link_to current_user.username, user_path(c.user) %><td>
	<td><%= c.content %><td>
	<td><%= c.updated_at %></td>
	<td><%= link_to "Edit", edit_article_comment_path(@article, c) , :remote=>true %></td>
	<td> <%= link_to "Delete", article_comment_path(@article, c), :method=>"delete", :remote=>true, :data=>{:confirm=>"Are you sure?"}, :id=>"comment-#{c.id}" %></td>
</tr>

